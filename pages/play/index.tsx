import { NextPage } from "next";

const Play: NextPage = () => {
        return (
            <div className={`flex flex-col mx-auto container bg-zinc-50`}>
                hello
            </div>
        );
}

export default Play;